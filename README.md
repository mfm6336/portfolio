# Fahad Ameen — Portfolio

This is a static portfolio website for **Fahad Ameen** (Oracle APEX Developer).  
Contains: `index.html`, `styles.css`, `script.js`, optional `assets/Fahad_Ameen_CV.pdf`.

## Quick setup (locally)
```bash
# 1. clone or create repository locally
git init
# add the files above into folder
git add .
git commit -m "Initial portfolio site"
git branch -M main
git remote add origin git@github.com:<your-username>/<repo-name>.git
git push -u origin main
